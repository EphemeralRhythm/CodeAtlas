<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginTop="20dp">

    <ImageView
        android:id="@+id/ring"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:visibility="invisible"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/map_icon_current"
        android:contentDescription="@string/ring" />

    <ImageView
        android:id="@+id/map_platform"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintEnd_toEndOf="@+id/ring"
        app:layout_constraintStart_toStartOf="@+id/ring"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/map_icon_enabled"
        android:contentDescription="@string/map_platform" />

    <ImageView
        android:id="@+id/icon"
        android:layout_width="42dp"
        android:layout_height="58dp"
        app:layout_constraintBottom_toBottomOf="@+id/map_platform"
        app:layout_constraintEnd_toEndOf="@+id/map_platform"
        app:layout_constraintHorizontal_bias="0.488"
        app:layout_constraintStart_toStartOf="@+id/map_platform"
        app:layout_constraintTop_toTopOf="@+id/map_platform"
        app:layout_constraintVertical_bias="0.0"
        app:srcCompat="@drawable/star"
        android:contentDescription="@string/star_icon" />

</androidx.constraintlayout.widget.ConstraintLayout>